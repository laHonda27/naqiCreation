import React from 'react';
import StaticDataManager from './StaticDataManager';

const GitSyncPanel: React.FC = () => {
  return (
    <div>
      <StaticDataManager />
    </div>
  );
};

export default GitSyncPanel;
